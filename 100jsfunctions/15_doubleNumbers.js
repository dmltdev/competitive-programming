/* 
15.doubleNumbers

Write a function named doubleNumbers that takes an array of numbers as a parameter. 
The function should return a new array where each element of the original array is doubled.
*/

function doubleNumbers(numbers) {
  return numbers.map(num => num * 2);   
}